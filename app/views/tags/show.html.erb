<div class="container">
  <div class="controls">
    <h1><%= @tag.try(:name) %></h1>
    <br>
    <p>Posts:</p>
    <ul>
    <% @tag.posts.each do |post| %>
      <li><a href="/posts/<%= post.id %>"><%= post.title %></a></li>
        <ul>
            <li><%= post.body %></li>
        </ul>
    <% end %>
    </ul>
<a href="/">Back</a>   
